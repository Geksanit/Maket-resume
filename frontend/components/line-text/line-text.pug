mixin line-text(options)
    if (!options || (typeof(options) !== 'object'))
        - options = {}
    - options.title = options.title || 'title'
    - options.date = options.date || 'date'
    - options.place = options.place || 'place'
    - options.text = options.text || 'text'

    li.line-text
        span.line-text__title #{options.title}
        span.line-text__date #{options.date }
        p.line-text__place #{options.place}
        p.line-text__text !{options.text}
