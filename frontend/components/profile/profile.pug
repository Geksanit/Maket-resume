include ../line/line
mixin profile(options)
    if !options
        - options = {}
    - options.text = options.text || 'text'
    - options.mix = options.mix || ''

    section(class="profile " + options.mix)
        +line(options)
        p.profile__text !{options.text}
