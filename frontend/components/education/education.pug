include ../header/header
include ../list-item/list-item

mixin education(options)
    - options = options || {}
    - options.list = options.list || []
    - options.mix = options.mix || ''

    section(class="education " + options.mix)
        .education__header
            +header(options)
        ul.education__list
            each item in options.list
                li.education__item
                    +list-item(item)
